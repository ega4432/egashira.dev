---
import { Icon as AstroIcon } from "astro-icon/components";

type Props = {
  name: string;
  class?: string;
};

const { name, class: className } = Astro.props;
---

<AstroIcon name={name} class={className} class="h-5 w-5" />
